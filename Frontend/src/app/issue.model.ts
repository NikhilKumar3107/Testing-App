// Defining an interface with similar structure in the back end.
export interface Issue {
    id: String;
    title: String;
    responsible: String;
    description: String;
    severity: String;
    status: String;
}
